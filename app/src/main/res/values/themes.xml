<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FERFUME" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@android:color/black</item>  <!-- Toolbar color (white) -->
        <item name="colorPrimaryDark">@android:color/black
        </item> <!-- Optional: Status bar color -->
        <item name="colorOnPrimary">@android:color/black</item>
    </style>

    <style name="Theme.FERFUME" parent="Base.Theme.FERFUME" />
</resources>